.git
.idea
# Docs internos (skills) y CI: no los usa el build y no tienen por qué ir en la imagen.
.claude
.github
tests
var
vendor
.gitattributes
.gitignore
.travis.yml
*.md
.php_cs.dist
.php_cs.cache
phpunit.xml.dist
*.md
# Certificado, logo y empresas.json se montan como volumen: un build local no puede
# llevarse los que haya en data/ (ni los .env locales).
data/*
!data/.htaccess
.env.local
.env.*.local
